version: "3.9"

services:
  api:
    build: .
    container_name: webscraping-exito-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - UV_COMPILE_BYTECODE=1
    volumes:
      - .:/app
